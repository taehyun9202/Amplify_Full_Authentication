import {} from "../types";

const initialState = {
  profile: {},
  token: "",
  loading: false,
};
export const profileReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
